# Poise 成品交付

这是当前可交付的成品版本，定位是：

- `iPhone / iPad` 浏览器可打开
- 可添加到主屏幕
- 本地保存训练进度、周总结、每日复盘
- 提供晨间全套、每日训练、自查图示、知识来源、提醒说明
- 已做离线缓存兜底：设备只要之前打开并缓存过，断网时更容易继续打开已缓存版本

当前目录中的核心文件：

- `index.html`
- `app.js`
- `styles.css`
- `manifest.webmanifest`
- `sw.js`
- `icon.svg`

当前唯一剩余阻塞：

- 如果要让手机在“你的电脑关机后”也始终可访问，需要一个稳定的静态托管入口
- 之前使用的临时分享链接已被托管端停用，所以它不适合作最终成品地址

这不是功能阻塞，而是发布阻塞。
